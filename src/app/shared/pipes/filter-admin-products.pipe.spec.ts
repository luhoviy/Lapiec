import { FilterAdminProductsPipe } from './filter-admin-products.pipe';

describe('FilterAdminProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAdminProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
